MPE Evolution Lineage
=====================

V0.9-Buffer:  Direct Translation Layer from V1.5RC2 of MPE
V1.5RC2:      v15R2 — Configuration System (69 tunables, registry, menu)
MFS:          MPE FTC Simulator — Physics-first pivot (parked (now consolidated in src/ecosystem/mfs/)
              during the MPE-only run)
              Milestone 1: Foundation & Robotics Core ✅
              Milestone 2: Physics Keystone (cylinders, anisotropic friction) ✅
              Milestone 3: Solver Hardening (in progress)

Current Head: v15R3 (MPE-only release, tagged; tree frozen to
              correctness/stability/validation/docs/hygiene)
Truth Pass:   Verlet-exact free flight, strict warm-start, true cylinder SDF,
              CCD remainder, no clamps/caps/hysteresis, F11 torture guardrails
              (gravity -17..-1, iters >=96, robustness verdict). 29/29 green.
Sleep Truth:  three-gate wake (first-touch pair novelty + fast-other + deep);
              F10 10-stack settles to all-asleep, KE=0 at 60 s.
TUI Suite:    mpe-tui live ncurses inspector + deterministic --snapshot /
              --stream dumps (demo/tower/pendulum/springlab/f10 scenes).
Adversarial:  f10_long_run (settle incl. run-max), sleep_contact_wake
              (slow-pusher first-touch + no-churn control), f11_torture
              (fixed-seed config extremes, corruption gates).
Spawning:     cylinders spawnable (Enter type 3, menu 8 settings, touch
               new.cyl): exact SDF contacts, ray selection, instanced mesh.

v15S Head:    GTK4 port (Wayland-safe input, gestures, overlay, dialogs).
Modularity:   MPI hot-plug ABI (shapes, broadphase, solver stages, tick
               modules); registry-first dispatch everywhere; per-world
               config authoritative in all hot paths; `mod` terminal
               command; TUI --broadphase/--solver flags; capsule example.
Data Layout:  growable bodies/contacts (ceilings kept), O(1) pair probes
               + id cache, islands O(J+B), small-first node pool,
               process-wide det counters. Suite v2 32/32 green
               (29 physics + 3 diag).
Modularity2:  lifetime-hardened registry/loader (tombstones, locks,
               builtin-takeover refusal, busy-unload, dladdr purge,
               origin-tracked hotload); per-stage mod_state; true segment
               capsule; no_collide proxies; ecosystem bundles loadable.
MFS:          tile floors everywhere; implicit motor solve + disturbance
               observer; torque-derived strafe + odom fusion; PTC fuse;
               per-world MEI; suite 8 gated + 5 info green.
Globals:      kernel holds zero sim state; primary owned by
               core/mpe_primary.c (app layer); registry process-global
               by design; UI/diagnostics stay app-global by design.

#ifndef mpe_version_h
#define mpe_version_h
/* GTK4-PREP: zero GUI headers in core. Version/release freeze were
 * previously in mpe_engine.h (which pulls gtk/gtk.h). Headless code
 * and validation need the version string without the GUI. */
#define a3_version_string "v15R3"
#define a3_release_freeze 1
#define a3_release_freeze_note "v15R3 release"
#endif /* mpe_version_h */
